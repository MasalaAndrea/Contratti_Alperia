.main-header {
    background: #f4f7fb;
    padding: 0;
    height: 220px;
    display: flex;
    align-items: center;
    justify-content: center;
}
.header-content {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 38px;
}
.logo { 
    width: 210px;
    height: 210px;
    object-fit: contain;
    display: block;
}
.logo-sinistra {
    margin-right: auto;
}
.logo-destra {
    margin-left: auto;
}
@media (max-width: 700px) {
    .main-header { height: 160px; }
    .logo { width: 120px; height: 120px; }
    .header-content { padding: 0 10px; }
}
.container {
    max-width: 600px;
    margin: 40px auto 0 auto;
    padding: 0 24px 24px 24px;
}
h1 {
    text-align: center;
    color: #1c3664;
    font-size: 3.2rem;
    margin-bottom: 10px;
}
.subtitle {
    text-align: center;
    color: #365fa0;
    font-size: 1.08em;
    margin-bottom: 28px;
}
.main-card, .card-centrale {
    background: #fff;
    box-shadow: 0 2px 16px rgba(0,34,85,0.09);
    border-radius: 15px;
    padding: 32px 22px 24px 22px;
    margin-bottom: 28px;
}
.section-contratto, .section-opzioni, .section-email {
    margin-bottom: 26px;
}
.section-contratto h2,
.section-opzioni h2,
.section-email h2 {
    font-size: 1.08em;
    color: #1a76d2;
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    gap: 10px;
}
.vertical-radio-group label {
    display: flex;
    align-items: center;
    gap: 7px;
    margin-bottom: 12px;
    font-size: 1em;
    color: #232c3d;
}
input[type="radio"] {
    accent-color: #1a76d2;
}
.settings-icon {
    font-size: 1.15em;
    cursor: pointer;
    color: #365fa0;
    margin-left: 6px;
    transition: color 0.2s;
}
.settings-icon:hover { color: #1a76d2; }
.main-btn {
    width: 100%;
    background: #1c3664;
    color: #fff;
    font-size: 1.15em;
    border: none;
    border-radius: 7px;
    padding: 13px 0;
    margin-top: 20px;
    margin-bottom: 10px;
    cursor: pointer;
    transition: background 0.2s;
    text-align: center;
}
.main-btn:hover { background: #174d90; }
.bozze-actions {
    margin-top: 22px;
    font-size: 0.97em;
    color: #888;
    text-align: center;
}
.bozza-btn {
    margin-top: 12px;
    background: #1c3664;
    color: #fff;
    border-radius: 8px;
    font-size: 1em;
    width: 100%;
    padding: 8px 0;
    transition: background 0.2s;
    border: none;
}
.bozza-btn:hover, .bozza-btn:active, .bozza-btn:focus {
    background: #174d90;
    color: #fff;
}
.main-footer {
    text-align: center;
    font-size: 0.95em;
    color: #aaa;
    padding: 18px 0 10px 0;
    margin-top: 42px;
    border-top: 1px solid #eaeaea;
}
.main-footer a {
    color: #365fa0;
    text-decoration: none;
    margin: 0 5px;
}
.selected-options-list {
    margin-top: 5px;
    display: flex;
    flex-wrap: wrap;
    gap: 5px;
}
.selected-option-pill {
    background: #e1ecfa;
    color: #1a76d2;
    border-radius: 13px;
    padding: 3px 13px;
    font-size: 0.97em;
    font-weight: 500;
    box-shadow: 0 2px 6px rgba(0,34,85,0.07);
    margin-bottom: 2px;
    display: inline-block;
}
.modal-overlay {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: rgba(83, 26, 26, 0.18);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
}
.modal-content {
    background: #fff;
    border-radius: 14px;
    box-shadow: 0 4px 24px rgba(0,34,85,0.13);
    padding: 24px 18px 18px 18px;
    min-width: 260px;
    max-width: 97vw;
    width: 100%;
    max-width: 400px;
    position: relative;
    animation: modalIn 0.27s;
}
@keyframes modalIn {
    from {transform: scale(0.7); opacity:0;}
    to {transform: scale(1); opacity:1;}
}
.close-modal {
    position: absolute;
    top: 8px;
    right: 16px;
    font-size: 2.1em;
    color: #888;
    cursor: pointer;
    font-weight: bold;
    transition: color 0.2s;
}
.close-modal:hover { color: #1a76d2; }
.opzioni-group {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin: 18px 0 18px 0;
}
input[type="text"],
input[type="email"] {
    width: 100%;
    padding: 10px 12px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 1.08em;
    box-sizing: border-box;
    outline: none;
    transition: border-color 0.2s;
    background: #f9f9fa;
    margin-bottom: 3px;
}
input[type="text"]:focus,
input[type="email"]:focus {
    border-color: #1c3664;
    background: #fff;
}
@media (max-width: 700px) {
    .container {max-width: 98vw;}
    .main-card, .card-centrale {padding: 16px 5vw;}
    .main-header { height: 70px; }
    .logo { height: 48px; margin-left: 12px; }
}
@media (max-width: 500px) {
    h1 { font-size: 1.35em !important; }
    .main-card, .card-centrale {padding: 7px 2vw;}
    .main-footer { font-size: 0.82em; }
}
.card-centrale {
    max-width: 540px;
    width: 96vw;
    margin: 32px auto;
    background: #fff;
    box-shadow: 0 4px 32px rgba(0,34,85,0.10);
    border-radius: 15px;
    padding: 30px 24px 22px 24px;
    display: flex;
    flex-direction: column;
    gap: 18px;
}
.titolo-contratto {
    text-align: center;
    color: #1c3664;
    font-size: 2.7rem;
    font-weight: 700;
    margin-bottom: 14px;
}
.sottotitolo {
    text-align: center;
    font-size: 1.25em;
    color: #274678;
    margin-bottom: 6px;
    font-weight: 500;
}
.form-group-doppio {
    display: flex;
    gap: 18px;
    align-items: flex-start;
}
.form-group-doppio .form-group {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
}
.form-group {
    margin-bottom: 18px;
    display: flex;
    flex-direction: column;
}
.form-group label {
    font-weight: 500;
    color: #1c3664;
    margin-bottom: 4px;
    line-height: 1.3;
    min-height: 22px;
}
.form-group input,
.form-group select {
    padding: 8px 10px;
    border: 1px solid #c1d0e4;
    border-radius: 6px;
    background: #f8f9fa;
    font-size: 1em;
    transition: border-color 0.18s;
    height: 40px;
    box-sizing: border-box;
    margin-bottom: 0 !important;
}
.form-group input:focus,
.form-group select:focus {
    border-color: #1a76d2;
    background: #fff;
}
@media (max-width: 700px) {
    .card-centrale {
        max-width: 99vw;
        padding: 18px 5vw;
    }
    .titolo-contratto { font-size: 1.55em; }
    .form-group-doppio {
        flex-direction: column;
        gap: 8px;
    }
}
.pod-input-group {
    display: flex;
    align-items: center;
    gap: 8px;
    min-height: 40px;
}
.pod-prefix {
    font-weight: bold;
    font-size: 1.08em;
    min-width: 25px;
    text-align: center;
    margin-bottom: 0;
}
.pod-small {
    width: 56px;
    min-width: 48px;
    max-width: 55px;
    padding: 6px 8px;
    font-size: 1em;
    border-radius: 5px;
    text-align: left;
    height: 40px;
}
.pod-large {
    width: 120px;
    min-width: 120px;
    max-width: 150px;
    padding: 6px 8px;
    font-size: 1em;
    border-radius: 5px;
    text-align: left;
    height: 40px;
}
@media (max-width: 700px) {
    .pod-input-group { flex-direction: column; gap: 2px; align-items: flex-start; }
    .pod-small, .pod-large { width: 98vw; min-width: 60px; max-width: 100vw; }
}
/* Radio row per Switch/Subentro/Voltura/Nuova Attivazione */
.radio-row {
    display: flex;
    gap: 16px;
    align-items: center;
    margin-bottom: 22px;
    flex-wrap: wrap;
}
.radio-row .checkbox-inline {
    display: flex;
    align-items: center;
    gap: 2px;
    margin-bottom: 0;
    white-space: nowrap;
    font-size: 1.08em;
    line-height: 1.2;
}
.radio-row input[type="radio"] {
    vertical-align: middle;
    margin-right: 4px;
}
@media (max-width: 700px) {
  .radio-row,
  .riscaldamento-row,
  .domestico-row {
    flex-direction: column;
    gap: 18px;
    align-items: flex-start;
  }
}
/* Domestico row energia */
.domestico-row {
    display: flex;
    flex-direction: column;
    gap: 0px;
    align-items: flex-start;
    margin-bottom: 32px;
}
.domestico-row .checkbox-inline {
     display: flex;
    align-items: center;
    gap: 2px;
    margin-bottom: 0;
    white-space: nowrap;
    font-size: 1.08em;
    line-height: 1.2;
}
/* Riscaldamento row gas */
.riscaldamento-row {
    display: flex;
    flex-direction: column;
    gap: 0px;
    align-items: flex-start;
    margin-bottom: 32px;
}
.riscaldamento-row .checkbox-inline {
    display: flex;
    align-items: center;
    gap: 2px;
    margin-bottom: 0;
    white-space: nowrap;
    font-size: 1.08em;
    line-height: 1.2;
}
/* Campo select più lungo e visibile */
.select-full {
    width: 100%;
    min-width: 320px;
    max-width: 480px;
    padding: 10px 14px;
    border-radius: 5px;
    font-size: 1.09em;
    border: 1px solid #c1d0e4;
    box-sizing: border-box;
    background: #f8f9fa;
    margin-bottom: 0 !important;
}
.form-group .select-full {
    margin-top: 6px;
    margin-bottom: 14px;
}

/* --- PAGINA 3: PAGAMENTO & CONSENSI --- */
.form-section {
    background: #f8fbff;
    border: 2px solid #d2e5f7;
    border-radius: 16px;
    padding: 16px 32px;
    margin-bottom: 22px;
    max-width: 600px;
}

.form-section h3 {
    font-size: 1.4em;
    margin-bottom: 12px;
    font-weight: bold;
    color: #234567;
}

/* Gruppi di opzioni in colonna con testo accanto */
.checkbox-group, .radio-group {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
}

/* Ogni opzione riga: radio/checkbox + testo di fianco */
.checkbox-group label,
.radio-group label {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 1.07em;
    cursor: pointer;
    width: 100%;
}

/* Input piccolo e allineato */
.checkbox-group input[type="checkbox"],
.radio-group input[type="radio"] {
    accent-color: #234567;
    width: 16px;
    height: 16px;
    min-width: 16px;
    min-height: 16px;
    margin-right: 0;
}

/* Per il gruppo Dichiarazioni: in colonna anche se sono radio */
.dichiarazioni-group {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
}

/* Responsive: mantiene tutto in colonna, testo accanto al radio/checkbox */
@media (max-width: 700px) {
    .form-section {
        max-width: 98vw;
        padding: 12px 8px;
    }
    .checkbox-group, .radio-group, .dichiarazioni-group {
        gap: 18px;
        align-items: flex-start;
    }
    .form-section h3 {
        font-size: 1em;
    }
}
/* Card colorate */
.card-dati-cliente { border-left: 5px solid #194e8a; }
.card-dati-tecnici { border-left: 5px solid #1aa07f; }
.card-consensi     { border-left: 5px solid #d85c25; }

/* Campi riepilogo */
.riepilogo-campo {
  margin-bottom: 14px; /* AGGIORNATO: spazio uniforme desktop e mobile */
  font-size: 1.07em;
  display: flex;
  align-items: baseline;
  flex-wrap: wrap;
}
.campo-nome {
  color: #194e8a;
  font-weight: 600;
  min-width: 150px;
  display: inline-block;
}
.card-dati-tecnici .campo-nome { color: #1aa07f; }
.card-consensi .campo-nome { color: #d85c25; }
.campo-valore {
  color: #333;
  font-weight: 400;
  margin-left: 12px;
  display: inline-block;
}
.card-title {
  margin-top: 0;
  margin-bottom: 14px;
  font-size: 1.24em;
  color: #222;
  font-weight: 600;
}
/* Card colorate e separate */
.section-box {
  background: #f9fafd;
  border: 2px solid #d1e4f6;
  border-radius: 12px;
  box-shadow: 0 2px 10px rgba(30,80,140,0.07);
  padding: 26px 20px 20px 20px;
  margin-bottom: 28px;
}

/* Bordo colorato per ogni card */
.card-dati-cliente { border-left: 6px solid #194e8a; }
.card-dati-tecnici { border-left: 6px solid #1aa07f; }
.card-consensi     { border-left: 6px solid #d85c25; }

/* Titolo card */
.card-title {
  margin-top: 0;
  margin-bottom: 14px;
  font-size: 1.24em;
  color: #1c3664;
  font-weight: 700;
}

/* Campo e valore */
.riepilogo-campo {
  margin-bottom: 14px; /* AGGIORNATO: spazio uniforme desktop e mobile */
  font-size: 1.07em;
  display: flex;
  align-items: baseline;
  flex-wrap: wrap;
}
.campo-nome {
  font-weight: 600;
  min-width: 160px;
  display: inline-block;
}
.card-dati-cliente .campo-nome { color: #194e8a; }
.card-dati-tecnici .campo-nome { color: #1aa07f; }
.card-consensi .campo-nome     { color: #d85c25; }
.campo-valore {
  color: #222;
  font-weight: 400;
  margin-left: 12px;
  display: inline-block;
}
/* --- BUSINESS: check/radio piccoli e a sinistra del testo --- */
.checkbox-group label,
.radio-group label,
.dichiarazioni-group label {
    display: flex;
    align-items: center;
    gap: 9px;
    font-size: 1.07em;
    cursor: pointer;
    width: 100%;
}

.checkbox-group input[type="checkbox"],
.radio-group input[type="radio"],
.dichiarazioni-group input[type="checkbox"],
.dichiarazioni-group input[type="radio"] {
    accent-color: #234567;
    width: 18px;
    height: 18px;
    min-width: 18px;
    min-height: 18px;
    margin-right: 0;
    margin-left: 0;
    box-shadow: none;
    border: 1px solid #b0c6e2;
    vertical-align: middle;
}

/* --- Bottoni Avanti/Indietro business --- */
.navigation-buttons {
    display: flex;
    gap: 18px;
    justify-content: space-between;
    margin-top: 18px;
}
.navigation-buttons button {
    background: #1c3664;
    color: #fff;
    border-radius: 7px;
    font-size: 1.05em;
    flex: 1 1 0;
    padding: 10px 0;
    border: none;
    cursor: pointer;
    transition: background 0.2s;
    min-width: 0;
    max-width: 100%;
    text-align: center;
    margin: 0;
}
.navigation-buttons button:hover {
    background: #174d90;
}
.navigation-buttons .home-btn-navi {
    background: #1976d2;
}
.navigation-buttons .home-btn-navi:hover {
    background: #125198;
}
@media (max-width: 600px) {
    .navigation-buttons {
        flex-direction: column;
        gap: 14px;
    }
    @media (max-width: 600px) {
  .navigation-buttons {
    flex-direction: column-reverse;
    gap: 14px;
  }
}
    .navigation-buttons button {
        width: 100%;
        font-size: 1em;
        padding: 12px 0;
    }
}
/* Allinea verticalmente check/radio e testo */
.checkbox-inline,
.radio-inline {
    display: flex;
    align-items: center;
    gap: 2px;           /* spazio tra input e testo */
    cursor: pointer;
    font-size: 1em;
    line-height: 1.2;
}
.card-energia {
    background: #f5faff;
    border-left: 6px solid #1a76d2;
    box-shadow: 0 2px 16px rgba(0,34,85,0.09);
    border-radius: 14px;
    padding: 30px 18px 20px 18px;
    margin-bottom: 28px;
}
.card-gas {
    background: #fff7f2;
    border-left: 6px solid #d85c25;
    box-shadow: 0 2px 16px rgba(218,92,37,0.09);
    border-radius: 14px;
    padding: 30px 18px 20px 18px;
    margin-bottom: 28px;
}
@media (max-width:700px) {
    .card-energia, .card-gas { padding: 15px 5vw; }
}
.checkbox-group label,
.radio-group label,
.dichiarazioni-group label {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 13px;           /* spazio tra input e testo */
    font-size: 1.07em;
    cursor: pointer;
    width: 100%;
    justify-content: flex-start;
}

.checkbox-group input[type="checkbox"],
.radio-group input[type="radio"],
.dichiarazioni-group input[type="checkbox"],
.dichiarazioni-group input[type="radio"] {
    accent-color: #234567;
    width: 18px;
    height: 18px;
    min-width: 18px;
    min-height: 18px;
    margin-right: 0;
    margin-left: 0;
    box-shadow: none;
    border: 1px solid #b0c6e2;
    vertical-align: middle;
}
.card-centrale {
    max-width: 540px;
    width: 96vw;
    margin: 32px auto;
    background: #fff;
    box-shadow: 0 4px 32px rgba(0,34,85,0.10);
    border-radius: 15px;
    padding: 30px 24px 22px 24px;
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.card-dati-cliente { border-left: 6px solid #194e8a; }
.card-dati-tecnici { border-left: 6px solid #1aa07f; }
.card-consensi     { border-left: 6px solid #d85c25; }

.card-title {
    margin-top: 0;
    margin-bottom: 14px;
    font-size: 1.24em;
    color: #1c3664;
    font-weight: 700;
}

.riepilogo-campo {
    margin-bottom: 14px; /* AGGIORNATO: spazio uniforme desktop e mobile */
    font-size: 1.07em;
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
}
.campo-nome {
    font-weight: 600;
    min-width: 160px;
    display: inline-block;
}
.card-dati-cliente .campo-nome { color: #194e8a; }
.card-dati-tecnici .campo-nome { color: #1aa07f; }
.card-consensi .campo-nome     { color: #d85c25; }
.campo-valore {
    color: #222;
    font-weight: 400;
    margin-left: 12px;
    display: inline-block;
}
.main-btn {
    width: 100%;
    background: #1c3664;
    color: #fff;
    font-size: 1.15em;
    border: none;
    border-radius: 7px;
    padding: 13px 0;
    margin-top: 20px;
    margin-bottom: 10px;
    cursor: pointer;
    transition: background 0.2s;
    text-align: center;
}
.main-btn:hover { background: #174d90; }
.bozza-btn {
    margin-top: 12px;
    background: #1c3664;
    color: #fff;
    border-radius: 8px;
    font-size: 1em;
    width: auto;
    padding: 8px 22px;
    transition: background 0.2s;
    border: none;
}

.bozza-btn:hover, .bozza-btn:active, .bozza-btn:focus {
    background: #174d90;
    color: #fff;
}
@media (max-width: 700px) {
    .card-centrale {
        max-width: 99vw;
        padding: 18px 5vw;
    }
    .card-title { font-size: 1.1em; }
    .riepilogo-campo {
        flex-direction: column;
        align-items: flex-start;
        margin-bottom: 14px; /* AGGIORNATO: spazio uniforme desktop e mobile */
    }
    .campo-nome {
        min-width: 0;
        margin-bottom: 0;
    }
    .campo-valore {
        margin-left: 0;
        margin-top: 2px;
    }
}
.home-btn {
    background: #1c3664;
    color: #fff;
    border-radius: 8px;
    font-size: 1em;
    width: 100%;
    padding: 8px 0;
    margin-top: 15px;
    margin-bottom: 8px;
    transition: background 0.2s;
    border: none;
    display: block;
    text-align: center;
    cursor: pointer;
}
.home-btn:hover, .home-btn:active, .home-btn:focus {
    background: #174d90;
    color: #fff;
}
.contatti-modal {
    position: fixed;
    z-index: 9999;
    left: 0;
    top: 0;
    width: 100vw;
    height: 100vh;
    overflow: auto;
    background: rgba(34, 40, 49, 0.25);
    display: flex;
    align-items: center;
    justify-content: center;
}

.contatti-modal-content {
    background: linear-gradient(127deg, #e6f0fa 60%, #fff 100%);
    padding: 32px 32px 20px 32px;
    border-radius: 16px;
    min-width: 280px;
    max-width: 96vw;
    box-shadow: 0 4px 28px rgba(0,0,0,0.13);
    text-align: center;
    position: relative;
    border: 1px solid #dbeafe;
    font-family: 'Segoe UI', Arial, sans-serif;
}

.contatti-close {
    position: absolute;
    right: 16px;
    top: 12px;
    font-size: 2em;
    font-weight: bold;
    color: #7b8fa1;
    cursor: pointer;
    transition: color 0.18s;
    z-index: 10;
}
.contatti-close:hover { color: #d85c25; }

.contatti-header {
    display: flex;
    align-items: center;
    gap: 12px;
    justify-content: center;
    margin-bottom: 4px;
}
.contatti-icon {
    font-size: 1.5em;
    color: #1a76d2;
}
.contatti-info {
    margin: 12px 0;
}
.contatti-nome {
    font-size: 1.1em;
    margin-bottom: 6px;
    color: #1a76d2;
}
.contatti-link-group {
    margin: 5px 0;
}
.contatti-link {
    text-decoration: none;
    color: #184e8e;
    font-size: 1em;
    transition: color 0.15s;
    border-radius: 4px;
    padding: 2px 6px;
}
.contatti-link:hover {
    color: #d85c25;
    background: #f9ece0;
    text-decoration: underline;
}
.contatti-link-ico {
    margin-right: 5px;
    font-size: 1.08em;
    vertical-align: middle;
}
.contatti-version {
    color: #999;
    font-size: 0.95em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 7px;
    margin-top: 0;
}
.contatti-ver-ico {
    font-size: 1.15em;
    color: #1a76d2;
    vertical-align: middle;
}
.firma-canvas {
    display: block;
    width: 100%;                 /* Si adatta al contenitore/card */
    aspect-ratio: 3/1;           /* Forma larga e bassa, ideale per la firma */
    max-width: 600px;            /* Limite su desktop */
    min-width: 170px;
    min-height: 60px;
    max-height: 200px;
    background: #ffffff;
    border: 3px solid #ff0000;
    border-radius: 0;
    margin: 0 auto 8px auto;
    box-sizing: border-box;
    touch-action: none;
}
@media (max-width: 700px) {
    .firma-canvas {
        min-height: 180px;   /* esempio: ora è più alto! */
        max-height: 240px;
        aspect-ratio: 3/1;   /* oppure lascia come preferisci */
    }
}
/* === BLOCCO RESPONSIVE E CENTRATO PER AGENTE E TIPO CONTRATTO === */
.contratto-flex {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 48px;
  flex-wrap: wrap;
  width: 100%;
}
.contratto-agente {
  display: flex;
  align-items: center;
  gap: 8px;
}
.contratto-radio-group {
  display: flex;
  flex-direction: column;
  align-items: center; /* CENTRA anche su desktop */
  width: 100%;
}
.contratto-titolo {
  font-weight: bold;
  font-size: 1.55em;
  margin-bottom: 18px; /* Più spazio sotto il titolo */
  text-align: center;
}
.contratto-radio-opzioni {
  display: flex;
  gap: 38px; /* Più spazio tra Residenziale e Business */
  justify-content: center;
}
.contratto-radio-opzioni label {
  font-size: 1.18em;
}

/* SOLO SU MOBILE: tutto in colonna e centrato */
@media (max-width: 600px) {
  .contratto-flex {
    flex-direction: column;
    gap: 16px;
  }
  .contratto-agente {
    justify-content: center;
  }
  .contratto-radio-group {
    align-items: center;
  }
  .contratto-radio-opzioni {
    flex-direction: column;
    gap: 12px;
    align-items: flex-start;
  }
}

/* === FINE BLOCCO RESPONSIVE E CENTRATO === */
/* Allineamento perfetto per la checkbox "Allega Condizioni Generali" */
.checkbox-allega {
    display: flex;
    align-items: center;
    gap: 8px;                /* Spazio tra checkbox e testo */
    font-size: 1.13em;
    font-weight: 500;
    color: #115890;          /* Colore blu come da esempio */
    cursor: pointer;
    user-select: none;
    margin: 0;
}

.checkbox-allega input[type="checkbox"] {
    width: 20px;
    height: 20px;
    accent-color: #115890;
    margin: 0 0 0 0;
    vertical-align: middle;
    flex-shrink: 0;
}

.checkbox-allega span {
    color: #115890;
    font-family: inherit;
    font-weight: 500;
    line-height: 1.2;
    margin: 0;
}

/* Responsive: mantiene allineamento anche su mobile */
@media (max-width: 600px) {
    .checkbox-allega {
        font-size: 1em;
        gap: 9px;
    }
    .checkbox-allega input[type="checkbox"] {
        width: 18px;
        height: 18px;
    }
}
.autocomplete-suggestions { background: rgb(3, 249, 52) !important; }